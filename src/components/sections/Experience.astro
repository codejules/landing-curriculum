---
import { work } from "@cv";

import Section from "@/components/Section.astro";
---

<Section title="Experiencia laboral">
    <ul>
        {
            work.map(
                ({
                    name,
                    position,
                    url,
                    startDate,
                    endDate,
                    summary,
                    highlights,
                }) => {
                    const startYear = new Date(startDate).getFullYear();
                    const endYear =
                        endDate !== null
                            ? new Date(endDate).getFullYear()
                            : "Actual";
                    const years = `${startYear} - ${endYear}`;

                    return (
                        <div>
                            <li>
                                <article>
                                    <header>
                                        <div>
                                            <a
                                                class="dark:text-white/80 text-black font-semibold"
                                                href={url}
                                                target="_blank"
                                                title={name}
                                                rel="noopener noreferrer"
                                            >
                                                {name}
                                            </a>
                                            {highlights && (
                                                <span class="highlights">
                                                    {highlights}
                                                </span>
                                            )}
                                            <h3 class="dark:text-white/80 text-black font-semibold">{position}</h3>
                                        </div>
                                        <time class="dark:text-white text-black">{years}</time>
                                    </header>
                                    <footer>
                                        <p class="dark:text-white/40 text-black/60">{summary}</p>
                                    </footer>
                                </article>
                            </li>
                        </div>
                    );
                },
            )
        }
    </ul>
</Section>

<style>
    ul {
        display: flex;
        flex-direction: column;
        gap: 2rem;
    }
    header {
        display: grid;
        margin-bottom: 0.7rem;
    }


    .link:hover {
        color: #555;
    }

    .highlights {
        background-color: #eaeaea;
        border-radius: 4px;
        font-size: 0.7rem;
        letter-spacing: 0.015rem;
        padding: 5px 8px;
    }

    time {
        color: #666;
        font-size: 0.9rem;
    }

    h3 {
        font-size: 1rem;
        font-weight: 300;
        padding-block: 0.2rem;
    }

    @container (width > 600px) {
        header {
            display: flex;
            flex-direction: row;
            justify-content: space-between;
            margin-bottom: 0;
        }
        h3 {
            padding-block: 0.7rem;
        }
    }
</style>
