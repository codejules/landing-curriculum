---
const { title } = Astro.props
---


<section>
{title && <h2 class="dark:text-white text-black">{title}</h2>}
<slot />
</section>

<style>
    section {
        container-type: inline-size;
        margin: 0 auto 32px;
        max-width: 700px;
    }

    h2 {
        font-size: 1.5rem;
        font-weight: 700;
        line-height: 1.5;
        margin-bottom: 1.5rem;
    }
</style>